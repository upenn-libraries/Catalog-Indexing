<%= bootstrap_form_for config_item, layout: :inline, remote: true do |f| %>
  <%= f.select :value, options, multiple: multiple, label: config_item.name.titleize %>
  <%= f.submit 'Save Setting' %>
<% end %>